apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus # dashboards in docker/grafana/dashboards/ reference this uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s # match the scrape interval (rate() windows >= 2x this)
