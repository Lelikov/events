# Per-object metrics: expose rabbitmq_queue_messages{queue=...} etc. on
# GET :15692/metrics (the aggregated default has no queue labels, which the
# Grafana dashboards need). Fine at this deployment's scale (~12 queues).
prometheus.return_per_object_metrics = true
